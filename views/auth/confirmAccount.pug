extends ../layout/index.pug

block contenido
    .py-10.animate-fade-in
        h1.text-5xl.my-10.font-extrabold.text-center Bienes
            span.font-bold Raices
        h2.text-center.text-3xl.font-extrabold #{pagina} 

    div(class=`py-8 px-4 rounded-lg max-w-md mx-auto animate-fade-in ${estado === 'error' ? 'bg-red-500' : 'bg-green-500'} text-white`)
        div(class="py-8 px-6 rounded-lg max-w-2xl mx-auto font-bold uppercase")
            p.text-sm.font-bold #{msg} 
            if estado === 'exito'
                a(href="/auth/login" class="text-center font-bold text-sm text-white uppercase mt-10 block") Ya puedes iniciar sesión
